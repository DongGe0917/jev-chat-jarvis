# Jev 聊天助手 (JevChat) 版本更新记录

---

## 📌 版本：v1.3 (最新版本)
**发布日期**：2026-09-22  
**构建版本号**：`versionCode = 4`, `versionName = "1.3"`  
**主要变更类型**：UI交互深度优化 · 卡顿修复 · 心理学校准增强 · 人设定制

### 🚀 核心优化与 Bug 修复
1. **彻底解决悬浮球点击卡顿与窗口开合失灵问题**：
   * **触控抖动误判修复**：重构了 `OverlayController` 中的手势识别逻辑，改用 Android 系统级 `ViewConfiguration.scaledTouchSlop` 代替原本极其狭窄的 6dp 阈值。彻底解决了用户点击图标时，手指轻微接触位移被系统误判为“拖拽中”导致点击事件被直接吞掉的问题。
   * **主线程高频阻塞消除**：在 `ChatCaptureService` 中对微信无障碍的 `TYPE_WINDOW_CONTENT_CHANGED`（界面内容变动）与 `TYPE_VIEW_SCROLLED`（滚动）事件增加了 **300ms 智能防抖节流**。原本每秒可能触发数十次耗时的微信完整控件树 DFS 遍历，现已降频合并，释放主线程运算压力，彻底杜绝微信界面掉帧与卡顿。
   * **触感与即时动画反馈**：悬浮球增加按下微缩放（0.92x）与抬起弹回动效，无论处于何种状态，点击都能第一时间获得视觉反馈。
   * **拖拽即时取消长按**：在手指识别出拖拽位移的第一时间自动注销 500ms 长按菜单监听，避免在拖拽中途意外弹出菜单导致界面冻结。

2. **深度内置 Jev 7 大心理学校准准则**：
   * 在调用国内大模型（如 DeepSeek 等）时，深度注入原版经过数百条实测校准的心理学 Prompt；
   * 包含中式潜台词（忠诚/在乎度试探 `confirm_you_care` vs 普通需求）、10 级冲突阶梯危险等级（0~9 分）、被质问时优先翻记录（`check_history`）等核心行动逻辑；
   * 强制候选回复字数在 5~35 字之间，真实自然，严禁假大空与公文风。

3. **新增【回复风格与人设定制】模块**：
   * 在设置中新增专属输入框，支持自由定义聊天人设；
   * 预置一键切换的推荐风格标签：`日常自然`（默认）、`幽默机智`、`温柔暖心`、`职场严谨`、`简短克制`。

---

## 📌 版本：v1.2
**发布日期**：2026-09-22  
**构建版本号**：`versionCode = 3`, `versionName = "1.2"`  
**主要变更类型**：多平台架构重构

### 🚀 核心优化
1. **Cherry Studio 风格多服务商支持**：
   * 告别单一的 OpenRouter 接口限制，新增对 **DeepSeek**、**OpenAI**、**硅基流动 (SiliconFlow)**、**月之暗面 (Moonshot / Kimi)**、**智谱 AI (GLM)** 及 **自定义 API (OneAPI/Ollama)** 的原生直连支持。
2. **多服务商独立配置存储**：
   * 切换服务商时独立保留各平台的 Base URL、API Key 与 Model，切换无需重新输入。
3. **连通测试升级**：
   * 设置页面支持基于当前所选平台进行实时的网络延迟与意图研判测试。

---

## 📌 版本：v1.0 ~ v1.1 (初始版本)
**主要特性**：
1. 基于系统无障碍服务的非侵入式聊天副驾内核；
2. 支持微信、QQ、X (Twitter)、飞书的消息采集；
3. 悬浮窗展示意图研判、危险等级与 3 条候选回复；
4. 一键填入聊天输入框（绝对不自动发送）。
